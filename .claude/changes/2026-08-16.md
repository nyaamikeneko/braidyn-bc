# 2026-08-16

## config.py: ローカル実行環境の対応を追加

- WSL (Windows Subsystem for Linux) 用の分岐を追加。`WSL_DISTRO_NAME` 環境変数で判定し、
  Google Drive を `/mnt/g/マイドライブ` として参照する。
- `DATA_NWB_ROOT` の探索先に `nwb_manual/<mouse_id>` を追加。
  従来の `.shortcut-targets-by-id/.../braidyn-bc/data` は実際のフォルダ名が
  `data_hackason`（NWB本体ではなく raw_data/videos 等）に変わっており、
  NWB ファイルの実体は `G:\マイドライブ\nwb_manual\VG1GC-66\...task-day15.nwb` にあることを確認。

## ローカル実行環境のセットアップ（WSL）

- `ssm`（lindermanlab）のビルドに MSVC が必要で Windows ネイティブでは失敗するため、
  WSL (Ubuntu, gcc同梱) 上に仮想環境 `.venv-wsl/` を作成して対応。
- `.venv-wsl` に `ssm`, `bdbc_nwb_explorer`, numpy/pandas/matplotlib/jupyter/nbclient/pynwb 等をインストール。
- note14 (`14_glmhmm_ver4_trials.ipynb`) を nbclient で全セル実行し、
  学習曲線・GLM重み・状態遷移などの図が正しく出力されることを確認済み。

## 今後の運用

- Claude Code がローカル(WSL)で note14 を編集・実行し、図を自分で確認したうえで次の変更に進む運用に変更。

## note14 の出力解釈とバグ発見

- ユーザーとの対話で note14 の実行結果（4次元・13次元GLM-HMMの重み、遷移行列、比較）を解釈。
- 4次元モデルで State2/State3 が自己遷移100%の吸収状態になっていた点、13次元モデルで State3 が
  実質未使用（縮退）になっていた点を指摘。
- `find_nwb_file()`（`src/glmhmm_ver4.py`）のNWB探索ログを精査した結果、day1処理時に
  day15のNWBファイル名が出力されていることに気づき、バグを特定：完全一致が失敗した際の
  フォールバック検索 `*{task_day}*.nwb` が単純な部分文字列一致のため、`task-day1` が
  `task-day15` に誤ヒットしていた。day1の「顔特徴あり」は誤りで、実際はday15のビデオ特徴を
  誤って読み込んでいた。

## バグ修正・note14 の再設計

- `find_nwb_file()` のフォールバックを、日番号の直後に数字が続かないことを保証する正規表現
  （`^.*{task_day}(?!\d).*\.nwb$`）に修正。単体テストで day1/day15 の誤マッチが解消したことを確認。
- ユーザー要望により note14 を再設計:
  - HMMを「全日プールした1系列」から「**日ごとに独立学習**」に変更（4次元・13次元とも）。
  - 13次元モデルは **K=2 と K=3 の両方**を日ごとに学習し、log-lik/trialで比較するように変更。
  - セクション6（4次元 vs 13次元の比較）も日ごと・K別の比較に対応させて全面書き換え。
  - `NWB_ROOT_OVERRIDE`（Colab専用の `/content/drive/...` パス）を `IN_COLAB` 判定で
    条件分岐させ、ローカル実行時に `config.py` の既定パスを壊さないよう修正。
- ローカル実行の可否をユーザーに問われ、`.venv-wsl` に `ssm`/`bdbc_nwb_explorer`/`pynwb` が
  インストール済みであること、`G:\マイドライブ\nwb_manual\VG1GC-66\` に day15 のNWBが、
  `G:\.shortcut-targets-by-id\...\hackathon_data` にCSVルートが実在することを確認し、
  WSL経由でのローカル実行が可能であると回答。
- 上記の変更・確認内容を `CLAUDE.md`（既知の落とし穴）、`docs/catalog.md`（note14の説明）、
  `docs/data.md`（NWBパスの記述）、`README.md`（K=3表記・ローカルWSL節）に反映。

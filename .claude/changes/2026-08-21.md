# 2026-08-21

## ノート16の新規作成: 学習B（顔つき13次元）を全task-dayに拡張

- ユーザーがドライブに `VG1GC-66` の全14 task-day分のNWB（`nwb_manual/VG1GC-66/`）を追加したことを確認（従来は `task-day15` の1件のみ）。
- `bdbc_nwb_explorer.read_nwb()` はデフォルト(`downsampled=True`)で `processing/downsampled` を読み、顔動画トラッキング（`face_video` entries）もそこに含まれる（`view.py`/`io.py` で確認）ため、`nwb_shrink` による軽量化版（`processing/behavior` 除去済み）でも13次元（顔特徴）モデルの学習に支障が無いことを確認。
- 方針をユーザーに提案して確認を取った上で `notebooks/16_glmhmm_ver4_faceB_alldays.ipynb` を新規作成:
  - ノート14の学習B（顔つき13次元）のみを対象とし、学習A（4次元）との比較・LL比較表は行わない。
  - 状態数はK=3固定（K=2との比較はしない）。
  - `VG1GC-66` の全14日を最初から通しで学習（`src/glmhmm_ver4.py` は無改造、既存のplot関数の組み合わせで対応）。
- WSL経由 (`jupyter nbconvert --to notebook --execute --inplace`) でノートを実行し、全14日がエラー無く学習できることを確認（`task-day1`/`task-day13` は3状態中1状態のみ使用の縮退あり、他12日は3状態とも使用）。
- コミット・pushの上、`master` へfast-forwardマージ（`65e3d87..9820a67`）。

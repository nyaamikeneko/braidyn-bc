# 2026-09-02

## Ver.5要件定義のレビュー（重大度つき）

ユーザーから「要件定義5に問題ある？重大度と一緒に教えて」と問われ、`docs/requirements_ver5.md` を `docs/requirements_ver4.md` / `docs/RQ.md` / `docs/data.md` / Cuturela原論文PDF / ローカルの `ssm` 実装と突き合わせて18点を指摘した。重大度で3段階に分けて提示。

**【重大】設計の前提に関わるもの**

1. **「日をまたぐ緩やかな変化」という5.2節の前提が、2.1節に自分で載せたデータに支持されていない**。No Reaction率のday index相関は Spearman −0.086 / Pearson −0.22 で単調傾向が無く、隣接day間の変化は最大0.54（day5→6）。$\alpha$ が大きい側に張り付いて実質day独立フィット（Ver.4）に戻る公算が高い。
2. **単一個体（VG1GC-66）であることが要件に書かれていない**。RQ2の主張がすべて n=1個体・14点の日回帰になる。
3. **系統Bの追加カテゴリの大半がセンサー由来**（Short Pullの97.2%が接触途切れ）。5.1節の「Successに至りやすい状態とShort Pullに落ちやすい状態を区別できる」という利得の説明が成り立たない。
4. **RQ1に残る循環**。状態事後確率は $y_t$ 自体から推論されるので「皮質から状態をデコード」は部分的に「皮質から行動をデコード」。6.4節のバランシングが唯一の防御なのに、動機として書かれているのは occupancy とdayの交絡だけ。
5. **H2・RQ3が「検証する」と書かれているのに検証できない**。6.7節がH2（RQ.md定義では皮質→表情のラグ）を検証するとあるが、その解析は7節でスコープ外。Step 10（Error Decomposition）に対応する仕様が6節のどこにも無い。

**【中】実装すると詰まる／結果が歪むもの**

6. cross-validationの単位が未定義。7. 入力が未標準化なのに $\alpha$ をスカラー1本に束ねている（Bias=1 に対し $x_{rew}$ の上限は5〜10）。8. 6節のデコード解析が試行数的に成立するか未検証。9. $P(z)\ge0.8$ 閾値が系統A/B比較の外部基準を壊す（残存試行数が揃わない）。10. Cuturelaの3段階フィット手順と20回初期化が反映されていない。11. day2欠損と「15日」表記（ランダムウォークのステップが実時間で2日空く）。12. Historyのday境界での扱いが未定義。

**【軽微】事実誤り・表記**

13. 「`ssm` はcategorical/multinomial GLM-HMMを標準搭載していない」は誤り。14. Cuturelaの公開実装URLは判明している。15. 5.5節の「4タイプ」消し忘れ。16. 4.3節／Step 11の判定に素の $\Delta_k$ を使っている（保持中時間はITI全体の9.3%あり、よく引く状態ほど $\hat r$ を過小評価する）。17. 5.3節のパラメータ数に初期状態分布の扱いが書かれていない。18. 2.1節「単一の系列」と5.2節「day単位のセッション」の書き分け。

なお13の確認過程で、ローカルの `.venv-wsl/lib/python3.12/site-packages/ssm/observations.py:629` の `InputDrivenObservations` が `C` を引数に取り、`Wk` の形が `(K, C-1, M)`、`calculate_logits()` が末尾にゼロ行を積む**参照カテゴリ固定**の実装であることを確認した。Ver.5 5.1節の系統Bと同じパラメータ化で、ガウス事前によるMAPとBFGSのMステップも実装済み。14はCuturela原論文PDFの Data and code availability から `https://github.com/lenca56/dynamic_glmhmm` を確認（この項目は並行して commit 54bedd5 で反映済みだった）。

## 指摘6点を要件定義に反映

ユーザーの指示により、3・4・6・7・10・13を `docs/requirements_ver5.md` に反映した。1・2・5・8・9・11・12・15・16・17・18 は未反映のまま残っている。

- **3（系統Bの解釈を「報酬が得られたか」に限定）**: §1・§4.1・§5.1・§5.5。§5.1に「SuccessとShort Pullの差を『引き方の質』と読んではならない」を根拠つき（Short Pull 509件中、測定 `pull_duration` が閾値未満は184件＝36.1%、残る325件の97.2%が接触途切れ）で明記し、系統Bの重みを運動学習の指標として使う解釈を禁止した。§5.5の「4タイプ」消し忘れ（指摘15）もここで直した。
- **4（RQ1の循環）**: §6.2に**cue前ベースライン窓を主解析**として追加し、pull onset基準窓を補助に降格。§6.4を「試行バランシング（循環への主対処）」に改題して役割を交絡除去と循環除去の2つに分離し、状態間比較を試行タイプ内に限定。§6.6のcross-condition decodingについては「train側・test側それぞれの内部で揃える（条件をまたいで揃えるのではない）」と書き分けた。§6.5のpermutationをバランシング層内・CV構造保持に限定。
- **6（CV仕様）**: §5.5に `cross-validationの仕様` を新設。公開実装の `split_data` / `evaluate` に合わせ、day内の連続ブロック（既定10試行）を5-foldに振り分け、test試行は系列から除去せず `present` マスクで欠測として前向き後向きに渡し、testマスクの立った試行の $\log c_t$ の平均を評価量とする。fold間SEの併記、History入力がtrainから完全独立でないことの明記、系統A/Bで同一foldを使うことも規定。
- **7（スカラー $\alpha$ と未標準化入力）**: §4.2に **History 2列を全日プールでz-score化**する規定を追加（day内で標準化するとRQ2で見たいday間変化が消えるため。CVではtrain側の統計を使う。重みの報告時は元スケールに戻す）。§5.2に「単一スカラーは入力列のスケールが揃って初めて意味を持つ」旨と、次元別 $\alpha_d$ への退避条件を追記。
- **10（多点初期化）**: §5.4の初期値に、Step (1) の静的フィットを乱数初期値を変えて複数回走らせる規定を追加。Cuturelaが(1)を全個体プールで学習しているのに対し本Verは同じ1個体・2359試行から作るため、初期値の情報量が乏しく多点化の重要性が高い点も明記。§5.5には探索の順序と温スタート、および**原論文の最適値 $\alpha\approx3$ を数値としては継承しない**（入力列のスケールが違うため）を追記。
- **13（`ssm` の多値対応）**: §5.4の「`ssm` がcategorical/multinomial GLM-HMMを標準搭載していない」を訂正し、`observation_kwargs=dict(C=3)` で系統Bの静的フィットが可能であること、参照カテゴリが**最後のクラス**なので No Reaction をindex 2 に置くこと、を明記。§5.1のHulsey比較にも「多値emission自体は `ssm` 本体にあり、Hulseyの改変は識別性の扱いの違いによるもの」を補足。

## ノート17（Ver.5実装前チェック）を新規作成し実行

「実装前にやると安い確認」4件を実行するノート `notebooks/17_ver5_pre_implementation_checks.ipynb` を新規作成し、WSLの `.venv-wsl` で nbconvert 実行（全30セル、出力・図をインライン保存）。Ver.4の試行テーブルから音提示試行のみを取り出してVer.5の系列（2359試行・14 day）を組み直し、Historyをその系列で計算し直して全日プールでz-score化している（History のday境界の扱いは要件に規定が無いため、Ver.4の `add_history` と同じくday頭でリセット）。`docs/catalog.md` にも項目を追加。

### 実測結果1: 報酬閾値が日ごとに動いている（要件に未記載）

| 指標 | day index との Spearman | p |
| :--- | ---: | ---: |
| No Reaction率 | −0.086 | .771 |
| Success率（全試行） | −0.354 | .215 |
| Success率（押下試行内） | **−0.670** | **.009** |
| `pull_duration_for_success` 中央値 | +0.069 | .815 |

`pull_duration_for_success` の中央値は day1 0.00 → day3 0.05 → day4 0.15 → day5–7 0.40 → day8 0.30 → day9 0.20 → day10 0.25 → day11–12 0.15 → day13 0.10 → day14–15 0.20 と動く。day1は閾値0秒（触れれば成功、押下試行内の成功率100%）。**Success / Reward の意味がday間で不変ではない**ため、押下試行内の成功率の有意な低下は学習の否定ではなく閾値ランプの反映と読むのが妥当。隣接day間のNo Reaction率の変化は平均0.175・最大0.540。

day6は日レベルの外れ値ではなく**セッション内の崩壊**だった（試行密度 z=+2.29、閾値0.40、日内3分割のNo Reaction率が 0.16 → 0.74 → 0.93）。5.2節がdayを単位に緩やかな変化を仮定する一方で、このデータの最大の変動はセッション内で起きている。

### 実測結果2: 系統Bの追加カテゴリには状態構造がほとんど無い

Short Pull（n=511）の3類型分解: dropout（接触途切れ）317件 62.0% / too_short 184件 36.0% / deadline 9件 1.8% / 測定不能 1件。閾値以上引けている325件のうち97.2%が接触途切れで `docs/data.md` の記述と一致。締め切りは No Reaction 試行の窓長中央値から実測（1.0000秒）。

| 系列 | runs test Stouffer z | lag-1 自己相関 | まとまり有意なday | 3次元入力のCV改善 |
| :--- | ---: | ---: | ---: | ---: |
| `yA`（反応したか） | −12.83 | +0.249 | 11/14 | +0.1058 nats/試行 |
| Short Pull \| 押下試行 | −2.34 | +0.047 | 1/13 | +0.0114 |
| Short Pull \| 押下試行・接触途切れ除外 | −2.88 | +0.079 | 1/13 | +0.0074 |

系統Aが説明する量に比べ、系統Bが追加で説明しようとしている境界は時間的まとまりも入力からの予測可能性も1桁弱い。接触途切れ型を除いても回復しない。

なお、この分解はギャップ埋め後の `pull_duration` に基づく。2026-08-31/09-01の監査で判明したとおり、CSVの `first_diff`（生 `state_lever` の連続保持時間、課題側の判定に対応）で測ると Short Pull の97%以上が閾値未達になるため、`first_diff` を正とするなら dropout の多くは too_short 側に移る。どちらで測っても「連続接触が途切れている」という機序は同じで、それがセンサー由来か握りの緩みかはデータからは区別できない（`docs/data.md`）。

### 実測結果3: 静的フィットの状態は「dayラベル」になっている

Ver.5 §5.4 Step (1) 相当の静的GLM-HMM（K=3・3次元入力・day単位の系列、`prior_sigma=2.0`、seeds=[0,1,2]、best log_prob −1004.26）を学習して確認した。

- $P(z)\ge0.8$ の残存率は全体84.4%（p_max平均0.927）だが、**day9は12.5%**。
- **day内で状態×試行タイプを釣り合わせた後に使える試行は14 dayすべてで0**。
- 閾値をかけない全試行で見ても、**1状態が90%以上を占めるdayが11/14**（うち11日は占有率ちょうど1.000）。**day内の状態切り替えは全期間で合計3回**（day6・day12・day14に各1回）、状態のrun長は中央値134〜156試行＝ほぼ1日まるごと。

つまりこのモデルが見つけている「内的状態」はセッション内の戦略切り替えではなく**日ごとの反応率の違い**で、状態変数がday識別子として使われている。6.4節のday内バランシングが空になるのはその直接の帰結。唯一の例外がday6で、状態の切り替わりが日内の崩壊と一致しており、十分大きい日内変化ならモデルが検出できることの裏付けにもなっている。

静的モデルにはdayごとの違いを表現する手段が状態変数しか無いので、状態がそこに吸われるのは構造的に自然。Dynamic GLM-HMMはday別GLM重みという専用の受け皿を持つため、day差が重みに吸収された結果として状態が日内の切り替えを担うようになる可能性はあり、その意味でこの結果は「Dynamic化が必要な理由」の実測的な裏付けでもある。

### 実測結果4: `ssm` の多値対応（指摘13の裏取り）

`InputDrivenObservations(self, K, D, M=0, C=2, prior_mean=0, prior_sigma=1000)`。`C=3` で系統Bの静的GLM-HMMがそのまま学習でき（best log_prob −2059.74、`Wk` shape `(3, 2, 3)`）、自前実装は不要。符号化の健全性確認として `yB` を2値に潰して `C=2` で学習すると log_prob −1004.26 となり、系統A（`v4.train_glmhmm_map`）の −1004.26 と**差 0.0009** で一致した。参照カテゴリを最後のクラスに置く規約（No Reaction = index 2）が正しいことが確認できた。

## 報酬閾値の変動が原論文に明記されていることを確認し、関連docを更新

ユーザーから「レバーの閾値が変動するってのは元論文に書いてるから探して」と指示され、`reference/sources/kondo2025_braidynbc_dataset_fulltext.pdf` のMethods "Tone-triggered lever-pull task" に該当記述を確認した。

> The required lever-pull time, Tpull, was adjusted according to the behavioral performance. The initial Tpull in the first session was set to 1ms. It was extended by 50ms when there was an 80% success rate in the previous 20 trials, up to the maximum duration of 400ms. We set a refractory period of 20 trials for these extensions... the initial Tpull in the next session was set to Tpull_final of the previous session minus 100ms. Tpull was never shortened within the same session.

同じ段落で、**キュー期間中に引けるのは1回だけ**であること（`T_pull` 経過前にnon-pulled状態へ戻ると、1秒の経過を待たずその時点でfailure確定・試行中断）と、**ITI中にレバーを引くとITIがさらに3–4秒延長される**ことも明記されていた。前者はSecond Pullが事実上発生しない理由（2026-09-01の監査で実測確認済み）を課題設計の側から説明する。後者は `docs/design_iti_poisson_emission.md` の有効露出 $\tilde\Delta$ の扱いに関係する（ITI長が押下に対して内生的）。

実測値（`VG1GC-66`）はこの規則と整合する。すべて50msの倍数、上限0.40秒、day内は非減少、day間で約100ms下がる。day1の0.00秒は初期値1msが記録精度で丸まったもので、この日は押下試行内の成功率が100%になる。

**最も重要な帰結**: staircaseが成功率をおよそ80%に保つよう働くので、**成功率は設計上おおむね安定し、学習曲線として使えない**。論文自身も学習の指標には成功率ではなくセッション終端の `Tpull_final` を使い、その時系列変化に基づく階層クラスタリング（Ward法）で25匹を2クラスタ（A: n=7、B: n=18）に分けている（Fig. 3c,d）。実測結果1で成功率にday傾向が出なかったのはこのためだった。

更新したファイル:

- **`reference/kondo2025_braidynbc_dataset.md`**: 要旨の「タスク」を「セッションを追うごとに課題が難化する設計」から適応的staircaseの記述に修正（上下に動く。成功率は学習指標にならない）。「記録系」節に `T_pull` の適応規則の全項目、キュー期間中1回だけの制約、ITI延長、論文の行動指標（response rate / success rate / `Tpull_final`）と2クラスタを追記。
- **`docs/data.md`**: 「`pull_duration_for_success`（= 論文の `T_pull`）は固定値ではない」節を新設。規則・`VG1GC-66` 全14日の実測表・3つの帰結（day間で報酬の意味が不変でない／成功率を学習曲線に使わない／閾値0の68件を閾値比較から外す）を記載。
- **`docs/requirements_ver5.md`**: §2.1の表に報酬閾値の行を追加し、3つの帰結を箇条書きで明記。§4.1の $Reward_k$ にも注意を付した。変更履歴に追記。
- **`CLAUDE.md`**: 既知の落とし穴に項目を追加。
- **`docs/RQ.md`**: RQ2に「学習段階の指標」を追加し、成功率ではなく $T_{pull}$ の軌跡を基準にすると明記。

## 要件への含意（未反映）

実測結果を踏まえた次の3点は設計判断を要するため、要件定義には反映していない（報酬閾値の件は上記のとおり反映済み。ただし「閾値をday共変量として持つか、閾値がほぼ一定の区間に限るか」の選択は未決定のまま記載している）。

1. **§6.4のバランシングに代替が要る**。候補は (a) day単位をやめてday群／全体でバランシング、(b) 日内で状態が振れるようKと入力を見直す、(c) day回帰を諦めて前半／後半の2群比較にする。あわせて $P(z)\ge0.8$ がday9をほぼ消す点の扱いを決める。§6.4・§6.7を確定させる前に、動的フィットで日内の状態切り替えが実際に生じるかを確認する必要がある。生じなければ状態＝dayなので「皮質から状態をデコード」は「皮質からどのdayかをデコード」と区別できず、RQ1が成立しない。
2. **系統Bは探索的位置づけへ格下げが妥当**（解釈の限定は反映済み）。§5.5に「系統Bを主系統に採る条件」を書くなら今回の数値が基準になる。
3. **日内の試行indexを扱う手当て**（入力に入れる／日内分割）の検討。

また §5.3 が併走を求めている $\alpha\to0$ ベースライン（＝今回のモデル）の状態は**解釈してはいけない**ことが確定した。
